# 🏗️ System Architecture

## 🎯 High-Level Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER INTERFACE                            │
│                                                                   │
│  ┌───────────────────────────────────────────────────────┐     │
│  │                   Frontend (HTML/CSS/JS)               │     │
│  │  • Image upload (drag & drop)                          │     │
│  │  • Prediction display                                  │     │
│  │  • Confidence visualization                            │     │
│  └────────────────────┬──────────────────────────────────┘     │
│                       │ HTTP POST /predict                      │
└───────────────────────┼─────────────────────────────────────────┘
                        │
                        ↓
┌─────────────────────────────────────────────────────────────────┐
│                      REST API (FastAPI)                          │
│                                                                   │
│  ┌───────────────────────────────────────────────────────┐     │
│  │  Endpoints:                                            │     │
│  │  • GET  /health      → Health check                    │     │
│  │  • GET  /classes     → List action classes             │     │
│  │  • POST /predict     → Image prediction                │     │
│  └────────────────────┬──────────────────────────────────┘     │
│                       │                                          │
└───────────────────────┼─────────────────────────────────────────┘
                        │
                        ↓
┌─────────────────────────────────────────────────────────────────┐
│                     PREPROCESSING                                │
│                                                                   │
│  Image → RGB → Resize(224,224) → Normalize[0,1] → Add Dims      │
│                                                                   │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ↓
┌─────────────────────────────────────────────────────────────────┐
│                   CNN-LSTM MODEL                                 │
│                                                                   │
│  Input: (batch, sequence, 224, 224, 3)                          │
│     ↓                                                            │
│  TimeDistributed(MobileNetV2) ← Pretrained ImageNet             │
│     ↓                                                            │
│  GlobalAveragePooling                                            │
│     ↓                                                            │
│  LSTM(128 units) + Dropout(0.3)                                 │
│     ↓                                                            │
│  Dense(256) + BatchNorm + ReLU + Dropout                        │
│     ↓                                                            │
│  Dense(128) + BatchNorm + ReLU + Dropout                        │
│     ↓                                                            │
│  Dense(15) + Softmax                                            │
│     ↓                                                            │
│  Output: (batch, 15) - Action probabilities                     │
│                                                                   │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ↓
┌─────────────────────────────────────────────────────────────────┐
│                   PREDICTION OUTPUT                              │
│                                                                   │
│  {                                                               │
│    "action": "dancing",                                          │
│    "confidence": 0.92,                                           │
│    "all_predictions": [                                          │
│      {"action": "dancing", "confidence": 0.92},                 │
│      {"action": "clapping", "confidence": 0.05},                │
│      ...                                                         │
│    ]                                                             │
│  }                                                               │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Training Pipeline

```
┌─────────────────┐
│  Raw Dataset    │
│  • CSV files    │
│  • Image folders│
└────────┬────────┘
         │
         ↓
┌─────────────────────────────────────┐
│  Phase 1: Data Exploration          │
│  • Analyze CSV structure            │
│  • Check class distribution         │
│  • Verify image paths               │
│  • Generate summary report          │
└────────┬────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────┐
│  Phase 2: Data Loading              │
│  • Load images                      │
│  • Resize to 224×224                │
│  • Normalize to [0,1]               │
│  • Apply augmentation               │
│  • Create train/val split           │
└────────┬────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────┐
│  Phase 3: Model Building            │
│  • Load MobileNetV2 (pretrained)    │
│  • Add LSTM layers                  │
│  • Add Dense layers                 │
│  • Compile with Adam optimizer      │
└────────┬────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────┐
│  Phase 4: Training                  │
│  • Batch processing                 │
│  • Forward propagation              │
│  • Loss calculation                 │
│  • Backpropagation                  │
│  • Weight updates                   │
│  • Callbacks (EarlyStopping, etc.)  │
└────────┬────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────┐
│  Phase 5: Evaluation                │
│  • Validation accuracy              │
│  • Confusion matrix                 │
│  • Classification report            │
│  • Per-class accuracy               │
│  • Visualizations                   │
└────────┬────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────┐
│  Trained Model                      │
│  • har_cnn_lstm.h5 (80 MB)         │
│  • label_encoder.pkl                │
└─────────────────────────────────────┘
```

## 🧠 Model Architecture Details

### MobileNetV2 CNN Backbone

```
Input: (224, 224, 3)
         ↓
┌───────────────────────┐
│  Conv2D (32 filters)  │
│  BatchNorm + ReLU6    │
└─────────┬─────────────┘
          │
          ↓
┌───────────────────────┐
│  Inverted Residual    │
│  Blocks (17 layers)   │
│  • Depthwise Conv     │
│  • Pointwise Conv     │
│  • Skip Connections   │
└─────────┬─────────────┘
          │
          ↓
┌───────────────────────┐
│  Conv2D (1280 filters)│
│  BatchNorm + ReLU6    │
└─────────┬─────────────┘
          │
          ↓
    Feature Map
    (7, 7, 1280)
```

### LSTM Temporal Layer

```
Feature Sequence
      ↓
┌─────────────────────┐
│  LSTM Cell          │
│  ┌───────────────┐  │
│  │  Forget Gate  │  │  ← Decides what to forget
│  └───────────────┘  │
│  ┌───────────────┐  │
│  │  Input Gate   │  │  ← Decides what to store
│  └───────────────┘  │
│  ┌───────────────┐  │
│  │  Output Gate  │  │  ← Decides what to output
│  └───────────────┘  │
│  ┌───────────────┐  │
│  │  Cell State   │  │  ← Long-term memory
│  └───────────────┘  │
└─────────┬───────────┘
          │
          ↓
  Hidden State (128)
```

## 📊 Data Flow in Prediction

```
User Uploads Image
       ↓
┌──────────────────┐
│ Read Image File  │ ← FileReader API (Frontend)
└────────┬─────────┘
         │
         ↓
┌──────────────────┐
│ Send to API      │ ← FormData POST request
└────────┬─────────┘
         │
         ↓
┌──────────────────┐
│ Receive in API   │ ← FastAPI receives UploadFile
└────────┬─────────┘
         │
         ↓
┌──────────────────────────┐
│ Preprocessing            │
│ • Convert to PIL Image   │
│ • Convert to RGB         │
│ • Resize to 224×224      │
│ • Normalize to [0,1]     │
│ • Add batch dim: (1,..)) │
│ • Add seq dim: (1,1,..)  │
└────────┬─────────────────┘
         │
         ↓
┌──────────────────┐
│ Model Inference  │ ← model.predict()
└────────┬─────────┘
         │
         ↓
┌──────────────────────────┐
│ Post-processing          │
│ • Get argmax for class   │
│ • Decode class name      │
│ • Get top-K predictions  │
│ • Format as JSON         │
└────────┬─────────────────┘
         │
         ↓
┌──────────────────┐
│ Send Response    │ ← JSON response
└────────┬─────────┘
         │
         ↓
┌──────────────────┐
│ Display Results  │ ← Update frontend UI
└──────────────────┘
```

## 🔄 Training Loop Details

```
For each epoch:
    ├─ For each batch:
    │   ├─ Load batch of images
    │   ├─ Apply augmentation
    │   ├─ Forward pass through model
    │   ├─ Calculate loss
    │   ├─ Backward pass (compute gradients)
    │   └─ Update weights
    │
    ├─ Calculate training metrics
    ├─ Validate on validation set
    ├─ Check callbacks:
    │   ├─ EarlyStopping: Stop if no improvement
    │   ├─ ModelCheckpoint: Save best model
    │   └─ ReduceLROnPlateau: Reduce learning rate
    │
    └─ Log metrics and continue
```

## 🎯 Loss Function: Categorical Crossentropy

```
For each sample:
    
    Ground Truth: [0, 0, 1, 0, 0, ..., 0]  ← One-hot encoded
                           ↑
                    (Class index = 2)
    
    Prediction:   [0.05, 0.10, 0.75, 0.05, 0.03, ..., 0.02]
                                ↑
                        (Predicted prob = 0.75)
    
    Loss = -log(0.75) = 0.288
    
Total Loss = Average of all sample losses
```

## 🚀 Deployment Architecture

```
┌───────────────────────────────────────────────────────────────┐
│                    Production Deployment                       │
│                                                                │
│  ┌─────────────┐      ┌─────────────┐      ┌─────────────┐  │
│  │   Frontend  │ ───→ │   API       │ ───→ │   Model     │  │
│  │   (Nginx)   │      │   (Gunicorn)│      │   (TF Serve)│  │
│  │   Port 80   │      │   Port 8000 │      │   Port 8501 │  │
│  └─────────────┘      └─────────────┘      └─────────────┘  │
│                                                                │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │               Load Balancer (Optional)                   │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                                │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │        Monitoring (Prometheus + Grafana)                 │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                                │
└───────────────────────────────────────────────────────────────┘
```

## 📈 Metrics Calculation

### Accuracy
```
Accuracy = (True Positives + True Negatives) / Total Samples
```

### Precision (per class)
```
Precision = True Positives / (True Positives + False Positives)
```

### Recall (per class)
```
Recall = True Positives / (True Positives + False Negatives)
```

### F1-Score (per class)
```
F1 = 2 × (Precision × Recall) / (Precision + Recall)
```

## 🎨 Frontend State Flow

```
Initial State
     ↓
┌─────────────────┐
│ Upload Section  │ ← User sees upload area
└────────┬────────┘
         │ User selects/drops image
         ↓
┌─────────────────┐
│ Preview Section │ ← Image preview shown
└────────┬────────┘
         │ User clicks "Recognize Action"
         ↓
┌─────────────────┐
│ Loading State   │ ← Spinner shows
└────────┬────────┘
         │ API responds
         ↓
┌─────────────────┐
│ Results Section │ ← Predictions displayed
└────────┬────────┘
         │ User clicks "Try Another"
         ↓
    Initial State
```

## 🔐 Security Flow

```
User Request
     ↓
┌─────────────────────┐
│  HTTPS/TLS          │ ← Encrypted connection
└──────────┬──────────┘
           │
           ↓
┌─────────────────────┐
│  Rate Limiting      │ ← Prevent abuse
└──────────┬──────────┘
           │
           ↓
┌─────────────────────┐
│  Input Validation   │ ← Check file type, size
└──────────┬──────────┘
           │
           ↓
┌─────────────────────┐
│  Authentication     │ ← Verify user (if needed)
└──────────┬──────────┘
           │
           ↓
┌─────────────────────┐
│  Process Request    │ ← Run prediction
└──────────┬──────────┘
           │
           ↓
┌─────────────────────┐
│  Sanitize Output    │ ← Clean response
└──────────┬──────────┘
           │
           ↓
     Send Response
```

---

**This architecture ensures:**
- ✅ Scalability (can add more API instances)
- ✅ Maintainability (modular design)
- ✅ Security (validation, HTTPS)
- ✅ Performance (efficient model, caching)
- ✅ Reliability (error handling, logging)
